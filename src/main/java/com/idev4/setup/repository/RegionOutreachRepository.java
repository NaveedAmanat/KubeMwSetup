package com.idev4.setup.repository;

import com.idev4.setup.domain.MwRegionWiseOutreach;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegionOutreachRepository extends JpaRepository<MwRegionWiseOutreach, Long> {
}
